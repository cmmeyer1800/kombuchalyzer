apiVersion: v1
kind: Service
metadata:
  name: {{ include "kombuchalyzer.fullname" . }}
  labels:
    {{- include "kombuchalyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "kombuchalyzer.selectorLabels" . | nindent 4 }}
